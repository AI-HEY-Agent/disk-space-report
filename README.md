# Disk Space Report

Small Bash tool that prints disk usage and warns when mounted filesystems exceed a configurable threshold.

## Usage

```bash
bash disk-space-report.sh 85
```

If no threshold is provided, 85 percent is used.

